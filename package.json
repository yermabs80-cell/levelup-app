{
  "name": "ethos-tracker",
  "version": "2.0.0",
  "private": true,
  "description": "Ethos Tracker — квесты, ранги и четыре пути развития (PWA без сборки)",
  "type": "module",
  "scripts": {
    "start": "http-server . -p 4173 -c-1",
    "deploy": "wrangler pages deploy . --project-name ethos-tracker",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "node --test \"test/*.test.js\""
  },
  "devDependencies": {
    "eslint": "9.15.0",
    "http-server": "14.1.1"
  },
  "engines": {
    "node": ">=20"
  }
}
